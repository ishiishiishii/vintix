# Go2-only dataset config for decoder-only finetuning (quadruped_locomotion group)

go2_walking_ad:
  type: "default"
  path: "go2_trajectories/data_1M"
  group: "quadruped_locomotion"
  reward_scale: 1.0
  episode_sparsity: 1
